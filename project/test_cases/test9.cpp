#include <bits/stdc++.h>
using namespace std;

int main ()
{
    int t;
    int t2;
    bool check = true;
    int a[100];
        
    a[0] = t;             //Since the variable t is not initialised so gives error variable not initialised
    t = 100;
    a[0] = t;              //This will work because t is initialised above

    return 0;
}